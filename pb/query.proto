syntax = "proto3";
package pb;
option go_package = "./pb";
// Query .
message Query {
	string pattern = 1;
	uint32 maxCount = 2;
	Searcher searcher = 3;
	repeated string paths = 4;
	repeated string types = 5;
}

enum Searcher {
	rg = 0;
	ag = 1;
}